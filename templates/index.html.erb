<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en-us">
  <head>
    <link href="http://gmpg.org/xfn/11" rel="profile">
    <meta http-equiv="content-type" content="text/html; charset=utf-8">

    <!-- Enable responsiveness on mobile devices-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1">

    <title>Projects</title>

    <!-- CSS -->
    <link rel="stylesheet" href="public/css/poole.css">
    <link rel="stylesheet" href="public/css/syntax.css">
    <link rel="stylesheet" href="public/css/site.css">
  </head>

  <body>

    <div class="container content">
      <div class="social-links">
      </div>
      <div class="masthead">
        <h3 class="masthead-title">
          <a class="title" href="/" title="Home">neopoly/OSS</a>
          <small class="tagline">
            &nbsp; neo unplugged &nbsp; &ndash; &nbsp;
            <span class="social-links">
              <a href="https://github.com/neopoly">GitHub</a>
            </span>
          </small>
        </h3>
      </div>

      <div class="posts">
        <% @languages.each do |language| %>
          <h1 class="language-title"><span><%= language %></span></h1>

          <% @projects.select { |p| p.language == language }.each do |project| %>
            <div class="post">
              <h1 class="post-title">
                <a href="https://github.com/<%= project.user_name %>/<%= project.repo_name %>"><%= project.repo_name %></a>
                <% project.contributors.each do |user| %>
                  <img src="<%= user['avatar_url'] %>" width="32" height="32">
                <% end %>
              </h1>
              <span class="post-date">
                <%= project.description %>
              </span>

              <% if project.badges && !project.badges.empty? %>
                <blockquote>
                  <% project.badges.each do |src| %><img src="<%= src %>"> <% end %>
                </blockquote>
              <% end %>
            </div>
          <% end %>
        <% end %>
      </div>

      <div class="footer">
        <p>
          &copy; <%= Time.now.year %>.
        </p>
      </div>
    </div>


  </body>
</html>
